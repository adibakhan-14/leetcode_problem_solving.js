// var smallestStringWithSwaps = function(s, pairs) {
//     var length= s.length;

// };

var arr= [[0,1], [2,3]];
arr.forEach(element => {
    for(let i=0; i<=element.length; i++ ){
        console.log(i, "ki pacchi");
    }    
});